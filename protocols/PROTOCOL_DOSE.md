# 사전 등록 — 지름길 용량-반응 + 재배선 대조 (2026-09-21, 실행 전 동결)

엔진 `evo_dose.py` sha256 앞 16자리 `dc27fa100d16d8a7` = `evo_swap.py`(98c0b5f9) + 단계별 이식 조건 AS<pct> + 가짜 재배선 SHAM (엔진 변경 38줄, 진화·평가 코드 불변). 뇌 데이터 `2781469e0524f33c`.

## 계기
게시 전 최종 심사(원고 v3.x)에서 남은 두 반론: (a) Gemini — 지름길 0.012% 대 10.6% 양 극단만 비교했으니 용량-반응을 보여라. (b) astra — 이식 조작은 지름길 외 간선도 바꾸므로, 재배선 양을 맞추되 지름길은 안 바꾸는 대조가 없으면 "경로 단축"은 해석에 그친다.

## 조건 (섬 6개 × 512, `--pop 3072 --conds A,AS1,AS3,AS5,AS10,SHAM`)
- A: 커넥톰.
- AS1·AS3·AS5·AS10: 커넥톰에 연결 수 보존 교환으로 냄새→하행 직결 비중을 1·3·5·10%까지 올림(시드0 실측 1.04·3.23·5.49·10.49%, 교환 17·31·46·100회). 활동 분포 SD 0.163→0.149~0.163, 침묵 38~47(커넥톰 48).
- SHAM: 전체 이식(10.6%)에 필요한 교환 횟수(101회)만큼 연결 수 보존 교환을 하되, 출발이 냄새 수용체가 아니고 도착이 하행 그룹이 아닌 간선 쌍만 교환. 지름길 비중 0.0001 불변, 입·출 연결 수 보존 확인.

## 설계
생태 P0T0·P1T0, 시드 0~9, 600세대, 결함 28 수정 엔진, global 보정, 평가·통계는 PROTOCOL_ECO와 동일. 20실행 × 약 40분 ≈ 13.5시간, 워크스테이션 단독.

## 사전 예측
- D1(주): 600세대 적합도가 지름길 비중에 단조 증가한다 — 시드별 (A, AS1, AS3, AS5, AS10) 순위에 대한 Page 검정(또는 Spearman 상관의 시드 평균) p < 0.05, 두 생태 평균.
- D2(주): SHAM − A는 ±0.10 동등 범위 안(시드 평균 90% 부트스트랩 구간). 즉 재배선 자체는 효과가 없다.
- D3: 후각 절제 비용이 지름길 비중에 단조 증가한다.
- D4: 0세대에서는 AS 단계 간 차이가 검출되지 않는다(효과는 진화로 커진다).
- 해석: D1·D2 지지 → 지름길이 효과의 원인이고 재배선 양은 아님. D2 기각(SHAM도 앞섬) → 효과의 일부는 재배선 일반의 결과이며 원고를 그렇게 고친다. D1 기각 → 단조성 없음, 임계 효과 또는 잡음으로 보고.
